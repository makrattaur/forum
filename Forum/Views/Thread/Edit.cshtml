@model Forum.Models.ThreadEditModel
@{
    ViewBag.Title = "Edit";
}

<h2>@Model.Thread.Title</h2>
<h3>Edit existing thread</h3>

@if (Model.Error != null)
{
    <div class="text-danger form-validation-error">@Model.Error</div>
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.ThreadId)

    <div class="form-group">
        @Html.LabelFor(m => m.Thread.Title, "Title: ")
        @Html.TextBoxFor(m => m.Thread.Title, new { @class = "form-control", style = "width: 300px;", @value = Model.Thread.Title })
    </div>
    
    <div class="form-group">
        @Html.LabelFor(m => m.Post.Content, "Message: ")<br />
        @Html.TextAreaFor(m => m.Post.Content, new { @class = "form-control", rows = "10", @value = Model.Post.Content })
    </div>
    
    <input type="submit" class="btn btn-primary" value="Edit" />
}

@Html.ActionLink("Back to thread", "Index", "Thread", new { id = Model.ThreadId }, null)

