@model IEnumerable<Database.User>
@{
    ViewBag.Title = "Users";
}

<h2>Users</h2>
<ul>
    @foreach (var user in Model)
    {
        <li>@user.Name</li>
    }
</ul>

@Html.ActionLink("Back to control panel", "Index")
